/* ==========================================================================
   Balance Nature Property Fund — backend configuration
   --------------------------------------------------------------------------
   Paste your Supabase project credentials below to switch the platform from
   local demo mode to real logins + shared, server-side data.

   Where to find these:
     Supabase dashboard -> Project Settings -> API
       * Project URL      -> supabaseUrl
       * anon / public key -> supabaseAnonKey   (safe to expose in the browser)

   Leave them blank to keep running in DEMO MODE (data stays in this browser
   via localStorage — exactly how the site behaves today).
   ========================================================================== */
window.SFF_CONFIG = {
  supabaseUrl: '',        // e.g. 'https://abcdefgh.supabase.co'
  supabaseAnonKey: ''     // e.g. 'eyJhbGciOi...'
};
